# Marketing Assistant

Internal tool for finding relevant X posts, drafting replies, and generating
daily growth digests. Built around a product profile system — the first profile
is **Signal Shift**, a calm AI-powered market briefing tool for retail investors.

Digests are generated by **Mel**, the internal marketing assistant.

---

## Project structure

```
marketing-assistant/
├── run_mock.py              # Entry point — mock mode (no API keys needed)
├── run_search.py            # Entry point — web search discovery (Tavily default, or Brave)
├── run_brave.py             # Entry point — Brave Search only (requires paid subscription)
├── run_x.py                 # Entry point — real X mode (requires X_BEARER_TOKEN, paused)
├── requirements.txt
├── .env.example             # Copy to .env for local email testing
├── .github/
│   └── workflows/
│       └── mel_digest.yml   # GitHub Actions workflow
├── profiles/
│   └── signal_shift.yaml    # Signal Shift product profile (search terms + fit keywords)
├── mock_data/
│   └── posts.py             # 8 fake X posts for mock mode
├── src/
│   ├── providers/
│   │   ├── mock_provider.py           # Returns mock posts and posting history
│   │   ├── tavily_search_provider.py  # Read-only Tavily Search discovery (preferred)
│   │   ├── brave_search_provider.py   # Read-only Brave Search discovery (subscription req'd)
│   │   └── x_provider.py              # Read-only X API v2 integration (paused)
│   ├── scorer.py            # Fit + Visibility + Opportunity scoring (dynamic for real posts)
│   ├── replier.py           # Generates 3 reply options per good-fit post
│   ├── post_generator.py    # Drafts original posts + schedule logic
│   ├── digest.py            # Assembles and saves the Markdown digest
│   └── email_sender.py      # Optional email delivery via Resend
└── output/
    └── daily_digest_YYYY-MM-DD.md   # Digest saved here
```

---

## Quick start — mock mode, no email

No API keys or configuration needed.

```bash
pip install -r requirements.txt
python run_mock.py
```

The digest is saved to `output/daily_digest_YYYY-MM-DD.md`.

---

## Real X mode — live post discovery

`run_x.py` fetches real recent X posts matching the Signal Shift profile,
scores them using keyword-based fit logic, and generates the digest.

**Safety:** Read-only. Nothing is posted, liked, followed, or replied to automatically.
Reply suggestions are for manual review only.

### Requirements

- An X Developer account with a project and app
- A Bearer token with **read-only** permissions (OAuth 2.0)

### Step 1 — Get an X Bearer token

1. Go to [developer.x.com](https://developer.x.com) and create a project + app
2. Under your app → **Keys and Tokens** → copy the **Bearer Token**
3. The Bearer token only needs read access — do not grant write permissions

### Step 2 — Add the token to your environment

**For local use**, add to your `.env` file:

```
X_BEARER_TOKEN=your_token_here
```

**For GitHub Actions**, add a repository secret named `X_BEARER_TOKEN`
(see GitHub Actions section below for the full secrets table).

### Step 3 — Configure your handles in the profile

Edit `profiles/signal_shift.yaml` and set your real handles:

```yaml
handles:
  founder: "@yourhandle"      # replace with real founder handle
  product: "@SignalShiftCo"   # replace with real product handle
```

The posting history check uses these handles to determine whether to skip
today's original posts.

### Step 4 — Run

```bash
python run_x.py
```

Mel fetches up to 10 recent posts from X, scores them, generates reply suggestions,
saves the digest, and (if `SEND_EMAIL=true`) sends the email.

### How fit scoring works in real mode

Mock mode uses hardcoded scores for 8 specific posts. Real mode uses keyword-based
scoring driven by `fit_keywords` in `profiles/signal_shift.yaml`:

| Tier | Logic |
|---|---|
| **Avoid** | Any avoid keyword present → hard stop, no engagement |
| **Weak fit** | Any weak keyword present → outweighs positive signals |
| **Strong fit** | 2+ strong keywords hit → high relevance |
| **Decent fit** | 1 strong or 1+ decent keyword → worth considering |
| **Weak fit** (fallback) | No matching keywords → assume misaligned |

Edit `fit_keywords` in the profile YAML to tune scoring for your product.

---

## Web search discovery mode — Tavily (preferred, no credit card)

`run_search.py` discovers recent X posts through a pluggable web search
provider. The default and recommended provider is **Tavily**, which has a
free tier that requires no credit card.

**Safety:** Read-only. Nothing is posted, liked, followed, or replied to.

### Why Tavily instead of the X API or Brave

| Provider | Billing | Credit card required | Notes |
|---|---|---|---|
| **Tavily** | Free tier available | No | Recommended starting point |
| Brave Search | Paid subscription | Required before API keys | Provider retained for future use |
| X API | $100/month (Basic) | Yes | Paused — no current billing |

### Requirements

- A free Tavily API key from [app.tavily.com](https://app.tavily.com)

### Step 1 — Get a Tavily API key

1. Go to [app.tavily.com](https://app.tavily.com)
2. Sign up for a free account (no credit card required)
3. Copy your API key — it starts with `tvly-`

### Step 2 — Add to your environment

Add to your `.env` file:

```
SEARCH_PROVIDER=tavily
TAVILY_API_KEY=tvly-your-key-here
MAX_SEARCH_QUERIES=5
MAX_RESULTS_PER_QUERY=5
```

### Step 3 — Run

```bash
python run_search.py
```

To use Brave Search instead:

```bash
SEARCH_PROVIDER=brave python run_search.py
```

### How many API calls per run

At most `MAX_SEARCH_QUERIES` calls (default: 5). One call per search term.
Printed at startup: `Max API calls this run: 5`.

### Limitations

| Limitation | Details |
|---|---|
| **No engagement metrics** | Likes, replies, reposts unavailable — scored as "Unknown visibility" |
| **Results may be older** | Search index may lag real-time X activity |
| **Snippets may be truncated** | Verify post quality before replying |
| **No posting history** | Cannot check when you last posted — review manually |
| **Tavily free tier limits** | Monthly request cap — see app.tavily.com for current quota |

All web-search-discovered posts are flagged:
- Visibility badge: **Unknown visibility** (amber)
- Engagement: `Engagement unknown · found via web search · ~Xh old`
- Email card: *"Found via Tavily Search — check engagement manually before replying."*

### How to use results safely

1. Run `python run_search.py`
2. Open each linked post URL to verify it exists and has real engagement
3. Check follower count and post quality before replying
4. Edit replies before posting — do not copy-paste without review
5. **Nothing is posted automatically**

---

## Brave Search discovery mode — retained, subscription required

`run_brave.py` discovers recent X posts using Brave's web search API instead
of the paid X API. No X billing required. No scraping. No login.

This is the recommended starting point if you want real post discovery without
setting up an X Developer account or paying for API access.

**Safety:** Read-only. Nothing is posted, liked, followed, or replied to.
            Reply suggestions are for manual review only.

### Why Brave Search instead of the X API

The X API's Basic tier (required for search) costs $100/month as of 2025.
Brave Search has a free/indie tier that allows a limited number of API calls
per month — enough to run a daily discovery session with a small query set.

Note: Brave Search API credits are separate from any Brave browser subscription.
The free tier may change over time — see [search.brave.com/search/api](https://search.brave.com/search/api)
for current pricing.

### Requirements

- A Brave Search API key (free tier available)

### Step 1 — Get a Brave Search API key

1. Go to [search.brave.com/search/api](https://search.brave.com/search/api)
2. Sign up and create an API key (the **Data for AI** or **Web Search API** plan)
3. Copy the key — it starts with `BSA...` or similar

### Step 2 — Add the key to your environment

**For local use**, add to your `.env` file:

```
BRAVE_SEARCH_API_KEY=your_key_here
MAX_SEARCH_QUERIES=5
MAX_RESULTS_PER_QUERY=5
```

**For GitHub Actions**, add a repository secret named `BRAVE_SEARCH_API_KEY`.

### Step 3 — Run

```bash
python run_brave.py
```

Mel runs up to `MAX_SEARCH_QUERIES` Brave Search queries (one per search term),
collects up to `MAX_RESULTS_PER_QUERY` results each, deduplicates by tweet ID,
scores the posts, generates reply suggestions, and saves the digest.

Total API calls per run: at most `MAX_SEARCH_QUERIES` (default 5).
A 1-second polite delay is added between queries.

### How search queries are built

Search terms come from `search_terms` in `profiles/signal_shift.yaml`.
Each term produces one Brave Search query:

```
site:x.com "retail investor noise"
site:x.com "information overload investing"
site:x.com "investing clarity"
...
```

Edit `search_terms` in the profile YAML to tune discovery coverage.

### Limitations

| Limitation | Details |
|---|---|
| **No engagement metrics** | Likes, replies, and reposts are unavailable — all scored as "Unknown visibility" |
| **Results may be older** | Brave's search index may lag real-time X activity |
| **Snippets may be truncated** | Brave returns a short text excerpt, not the full post |
| **Post quality varies** | Not all results will be high-quality — review before replying |
| **No posting history** | Brave mode cannot check when you last posted — review schedule manually |

Posts discovered via Brave Search are flagged in the digest and email:

- Visibility badge shows **"Unknown visibility"** (amber)
- Engagement line reads: `Engagement unknown · found via web search · ~Xh old`
- Email card shows: *"Found via Brave Search — check engagement manually before replying."*

### How to use results safely

1. Run `python run_brave.py` to generate the digest
2. Open the linked X post URL in a browser to verify it exists and has engagement
3. Check the author's follower count and post quality before replying
4. Use the A/B/C reply options as a starting point — edit before posting
5. **Do not reply automatically** — all replies are manual only

---

## Enable email delivery

Email is off by default (`SEND_EMAIL=false`). To turn it on:

### 1. Get a Resend account

Sign up at [resend.com](https://resend.com). The free tier allows 100 emails/day.

### 2. Create an API key

In the Resend dashboard → **API Keys** → create a new key with sending permission.

### 3. Verify a sending domain

In the Resend dashboard → **Domains** → add and verify your domain.

For initial testing, Resend provides a shared sandbox address:
`onboarding@resend.dev` — you can send from this without domain setup, but
delivery is limited to your own verified email address in Resend.

### 4. Create your .env file

```bash
cp .env.example .env
```

Then edit `.env`:

```
SEND_EMAIL=true
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_your_key_here
EMAIL_FROM=Mel <mel@yourdomain.com>
EMAIL_TO=you@example.com
```

### 5. Run

```bash
python run_mock.py
```

Mel will generate the digest, save it locally, and send the email.

---

## How to test email safely

1. Set `EMAIL_TO` to your own email address — never a customer or public address.
2. Run once and check your inbox (check spam if it doesn't arrive).
3. Confirm delivery in the Resend dashboard at **resend.com/emails**.
4. The digest is always saved locally to `output/` even if email fails — nothing
   is lost if the send does not go through.

---

## Running without a .env file

You can also pass env vars inline:

```bash
# Digest only (default)
python run_mock.py

# Digest + email (inline)
SEND_EMAIL=true RESEND_API_KEY=re_xxx EMAIL_FROM=mel@yourdomain.com EMAIL_TO=you@example.com python run_mock.py
```

---

## What the digest contains

1. **Today's Best 3** — top posts worth replying to, filtered by fit and opportunity
2. **8 mock X posts** scored across three dimensions:
   - **Fit** — Strong / Decent / Weak / Avoid
   - **Visibility** — Strong / Decent / Low (raw engagement metrics)
   - **Opportunity** — High / Medium / Low / Poor (fit + timing + audience quality)
3. **Why it is or is not worth engaging** for each post
4. **3 reply options** (A/B/C) for every Strong or Decent fit post
5. **Suggested account** — Founder, @SignalShiftCo, Either, or Do not reply
6. **Media suggestion** per reply — No media / Optional GIF / Quote repost / etc.
7. **Original post drafts** — schedule-aware, with format and media guidance
8. **Posting history awareness** — skips posts if an account already posted today

---

## Email vs Markdown artifact

Mel produces two outputs every run:

| Output | What it contains | Where it goes |
|---|---|---|
| **HTML email** | Today's Best 3 — all reply options, media guidance, quick links | Sent via Resend if `SEND_EMAIL=true` |
| **Markdown artifact** | All 8 posts — full scoring detail, all reply options, original post drafts | Saved to `output/` and uploaded as a GitHub Actions artifact |

### What the HTML email includes per card

- Fit, visibility, and opportunity badges
- Engagement summary
- **Recommended reply** highlighted (the best option, copy-ready)
- **Other reply options** shown below so you can pick the right tone
- **Media guidance** — type, specific GIF idea or use-case, when to use it, when to skip it
- "Open post ↗" and "View profile" buttons

The email is designed for mobile: scan, pick a reply, open the post, done.
The Markdown artifact is the complete reference for every post in the run.

---

## Where digests are saved

```
output/daily_digest_YYYY-MM-DD.md
```

One file per day. Rerunning the same day overwrites the existing file.

The `output/` directory is listed in `.gitignore` — generated digest files are never committed to the repository. The directory is created automatically at runtime if it doesn't exist.

For saved runs, use the GitHub Actions artifact (uploaded every run regardless of whether email is enabled). Do not commit output files manually.

---

## GitHub Actions — automated digest and email

The workflow at `.github/workflows/mel_digest.yml` runs Mel on GitHub's
infrastructure. It can be triggered manually at any time, and optionally
scheduled for daily runs.

> **Security reminder:** Never put API keys in code, workflow files, or chat.
> Use GitHub Secrets exclusively. Nothing in this repo contains real credentials.

### Step 1 — Add GitHub Secrets

Go to your repository on GitHub:

**Settings → Secrets and variables → Actions → New repository secret**

Add each of these secrets:

| Secret name | Required for | Value to set |
|---|---|---|
| `SEND_EMAIL` | Email delivery | `true` to email the digest, `false` to skip email |
| `EMAIL_PROVIDER` | Email delivery | `resend` |
| `RESEND_API_KEY` | Email delivery | Your Resend API key (starts with `re_`) |
| `EMAIL_FROM` | Email delivery | e.g. `Mel <mel@yourdomain.com>` |
| `EMAIL_TO` | Email delivery | The address you want the digest sent to |
| `X_BEARER_TOKEN` | Real X mode only | Your X API v2 Bearer token (read-only) |
| `BRAVE_SEARCH_API_KEY` | Brave Search mode only | Your Brave Search API key |
| `MAX_SEARCH_QUERIES` | Brave Search mode only | Number of queries per run (default 5) |
| `MAX_RESULTS_PER_QUERY` | Brave Search mode only | Results per query (default 5) |

`X_BEARER_TOKEN` and `BRAVE_SEARCH_API_KEY` are only needed if you switch the
workflow to use `run_x.py` or `run_brave.py` respectively.
The default workflow uses `run_mock.py` and does not require either.

You can set `SEND_EMAIL=false` first to test the workflow runs cleanly before
wiring up email.

### Step 2 — Run the workflow manually

From a browser or the GitHub mobile app:

1. Go to your repository
2. Tap the **Actions** tab
3. Select **Mel's Daily Digest** from the left sidebar
4. Tap **Run workflow** (top right of the workflow runs table)
5. Leave the branch as `main` and tap the green **Run workflow** button

The run will appear in the list within a few seconds. Tap it to watch the logs.

### Step 3 — Download the digest

Every run uploads the digest as a GitHub Actions artifact regardless of whether
email is enabled. To download it:

1. Open the completed workflow run
2. Scroll to the **Artifacts** section at the bottom
3. Download **mel-digest-\<run-id\>** — it contains the Markdown digest file

### Enabling scheduled daily runs

The workflow includes a daily schedule that is commented out by default.
To enable it, edit `.github/workflows/mel_digest.yml` and uncomment these lines:

```yaml
  # schedule:
  #   - cron: '0 8 * * 1-5'  # weekdays at 8am UTC
```

Change the cron expression to suit your timezone and preferred run time.
[crontab.guru](https://crontab.guru) is useful for checking cron syntax.
